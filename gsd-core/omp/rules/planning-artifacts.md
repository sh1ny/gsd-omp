# GSD Planning Artifacts

When a workspace contains `.planning/`, treat it as the authoritative GSD workflow state. Read `.planning/STATE.md` before continuing a workflow, preserve milestone and phase artifacts unless the user explicitly asks to delete them, and keep generated GSD artifacts separate from user-authored project files.
